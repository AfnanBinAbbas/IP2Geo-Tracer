# IP2Geo-Tracer
CIDR geolocation tool with optional region filtering. Queries ip-api.com for each CIDR's first IP, saves JSON results, and can filter CIDRs by state/region names (e.g., naval bases in India).
